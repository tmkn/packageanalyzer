describe(`PackageAnalytics Tests`, () => {
    it(`Check licenses`);
    it(`Checks package with most direct dependencies`);
    it(`Checks package that is most referred`);
    it(`Checks for package with most versions`);
});
