import * as assert from "assert";

describe(`flatFolderProvider Tests`, () => {
    it(`Resolves a package`);
    it(`Resolves a package with version`);
    it(`Resolves a package with semantic version syntax`);
    it(`Resolves multiple packages`);
});