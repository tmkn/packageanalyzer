import { Package } from "../src/package/package";

describe(`Collector Tests`, () => {
    test.todo(`correctly collects data`);
    test.todo(`flattens correctly with no key arg`);
    test.todo(`flattens correctly with custom key arg`);
});
